# 低代码前端平台 (Low-Code Frontend Platform) - 0.1.0 规划文档

## 1. 版本定位与核心愿景

在 **0.0.1 版本**中，我们成功完成了基于 `vue3-grid-layout` 的可视化网格画布、高端表单 (ProForm) 与高端表格 (ProTable) 物料拖拽、右侧抽屉配置以及 JSON 文件/SQLite 审计日志的双存储架构。

**0.1.0 版本** 的核心突破点在于**解决“数据联通与网络请求”问题**：
1. **数据源与 API 接口接入**：组件属性如何优雅地与后端 RESTful API / GraphQL 接口建立连接？
2. **请求参数与字段映射 (Parameter & Field Mapping)**：如何将页面组件的值（如表单筛选项、全局状态、路由参数）动态映射为 API 请求参数，并将 API 返回数据精确映射到表格/组件字段上？
3. **人性化交互设计**：如何在侧边配置抽屉中提供极佳的可视化交互体验，让不写代码的用户也能完成复杂的接口绑定与字段映射？

---

## 2. 核心 Schema 扩展设计 (Data Source & Api Binding Schema)

为了支撑数据源连接与传参映射，我们需要扩展 `PageSchema` 与 `ComponentNode`，引入全局数据源 `dataSources` 与节点数据绑定 `dataSourceBinding`。

### 2.1 页面全局数据源定义 (`DataSourceSchema`)

```typescript
// 页面全局 API 数据源列表定义
export interface DataSourceSchema {
  id: string;               // 数据源唯一 ID (例: "ds_get_user_list")
  name: string;             // 别名/描述 (例: "查询用户列表API")
  url: string;              // 请求 URL (支持动态插值，如 "/api/users/{userId}")
  method: 'GET' | 'POST' | 'PUT' | 'DELETE';
  headers?: Record<string, string>;
  // 默认请求参数定义
  defaultParams?: Record<string, any>; // Query 或 Body 默认参数
  // 响应数据预处理 (可选 JS 表达式/函数)
  dataTransform?: string;    // 例如: "res.data.items" 或 "res.list"
}

// 扩展后的 PageSchema
export interface PageSchema010 {
  id: string;
  title: string;
  type: 'page' | 'component';
  meta: { author: string; description: string; version: string };
  // 页面全局响应式状态变量 (如 searchForm: { name: '' })
  state: Record<string, any>;
  // 全局数据源列表
  dataSources: DataSourceSchema[];
  // 画布组件节点
  children: ComponentNode010[];
}
```

### 2.2 组件节点 API 绑定模型 (`DataSourceBinding`)

```typescript
export interface DataSourceBinding {
  dataSourceId: string;     // 关联的全局 API 数据源 ID
  autoFetch: boolean;       // 是否初始化时自动调用
  // 1. 请求参数映射：如何把表单项或 State 映射到 API 的 query/body 传参
  paramMapping: {
    apiKey: string;         // 接口定义的参数名 (例: "keywords", "page_num")
    sourceType: 'state' | 'component_value' | 'static' | 'route_param';
    sourceValue: string;    // 对应的来源 (例: "searchForm.username" 或 "node_input_1.value")
  }[];
  // 2. 响应字段映射：如何把接口返回数据填充到当前组件的特定属性/列上
  responseMapping: {
    targetProp: string;     // 组件的目标属性 (例: "mockData" 或 "config.columns")
    dataPath: string;       // 响应 JSON 提取路径 (例: "data.records" 或 "data.total")
  }[];
}

export interface ComponentNode010 {
  id: string;
  type: string;
  label: string;
  layout: { x: number; y: number; w: number; h: number; i: string };
  props: Record<string, any>;
  attrs: Record<string, any>;
  config?: Record<string, any>;
  // 核心扩展：API 数据源绑定配置
  apiBinding?: DataSourceBinding;
}
```

---

## 3. 传参映射与数据通信机制 (Parameter & Data Mapping)

实现组件属性与接口连接的核心在于“解耦”与“响应式追踪”：

```
+-------------------+             +--------------------+             +------------------+
| 组件A / 搜索表单   | 变量改变 -> | 页面全局 State Store | -> 触发请求  | 数据库 / Backend |
| (ProForm)         |             | (Pinia State)      |             | RESTful API      |
+-------------------+             +--------------------+             +------------------+
                                           │                                  │
                                     参数映射处理                        返回 JSON 响应
                                           │                                  │
                                           ▼                                  ▼
                                  +-------------------+              +------------------+
                                  | 封装组装 HTTP 请求 | ------------ | 数据映射器        |
                                  | (Axios/Fetch)     |              | (ResponseMapper) |
                                  +-------------------+              +------------------+
                                                                              │
                                                                         字段提取与填充
                                                                              │
                                                                              ▼
                                                                     +------------------+
                                                                     | 组件B / 高端表格  |
                                                                     | (ProTable Data)  |
                                                                     +------------------+
```

### 3.1 传参映射模式 (Request Parameter Mapping)
1. **静态常量 (Static)**：直接固定参数值，如 `pageSize: 10`, `status: "ACTIVE"`。
2. **全局状态联动 (State Bound)**：绑定到页面 `state.searchForm.keyword`，表单项变动时 API 参数自动同步。
3. **跨组件驱动 (Component Bound)**：指定监听另一个组件的值（如当选定 `Select` 树组件节点时，自动触发表格 `table_1` 刷新并带入 `dept_id`）。
4. **URL / 路由传参 (Route Param)**：获取页面 URL 查询参数（如 `?userId=1001`）。

### 3.2 响应映射与数据转换 (Response Field Mapping)
不同后端返回的数据格式可能千差万别（有的返回 `{ code: 200, data: [...] }`，有的返回 `{ result: { list: [...] } }`）。
- **字段路径选择器 (JSON Path Parser)**：允许用户输入 `data.records` 或使用可视化的 JSON 树挑选目标数组。
- **自定义数据转换函数 (JS Expressions)**：预留极简代码转换器，如 `data => data.map(item => ({ ...item, fullname: item.first_name + item.last_name }))`。

---

## 4. 人性化可视化交互设计方案 (UX/UI Design)

要在零代码/低代码环境下让非技术人员顺畅配置接口，推荐采用 **三步走引导式抽屉 (3-Step Drawer Wizard)** 交互模式：

### 4.1 抽屉面板“数据源 (Data Source)”Tab 布局结构

在组件配置抽屉 (`PropertyDrawer.vue`) 中新增 **【数据源 API】Tab**：

```text
+-------------------------------------------------------------+
| [属性 Props] [高级 Config] [数据源 API ★] [原生 Attrs] [JSON]  |
+-------------------------------------------------------------+
|  启用接口数据联通: [ 开关 ON/OFF ]                            |
|                                                             |
|  1. 选择 API 数据源                                          |
|     下拉选择: [ GET /api/users - 查询用户列表          ▼ ]   |
|     + 新增全局数据源接口                                      |
|                                                             |
|  2. 配置请求传参映射 (Params Mapping)                       |
|     +-----------------------------------------------------+ |
|     | API 参数名    | 来源类型    | 绑定的变量 / Component | |
|     |-------------+-----------+-------------------------| |
|     | page        | 静态常量   | [ 1                  ]  | |
|     | keyword     | 全局State  | [ state.searchForm.kw▼] | |
|     | categoryId  | 关联组件   | [ form_1.category    ▼] | |
|     +-----------------------------------------------------+ |
|     + 添加参数映射项                                         |
|                                                             |
|  3. 配置响应数据填充 (Response Mapping)                      |
|     目标组件属性: [ 表格数据源 (mockData)                ▼ ] |
|     JSON 提取路径: [ data.records                       ] |
|     [ ⚡ 现场测试接口请求与数据预览 ]                       |
|                                                             |
|  +-------------------------------------------------------+  |
|  | 调试结果预览 (Response Preview):                       |  |
|  | { "code": 200, "data": { "total": 100, ... } }        |  |
|  +-------------------------------------------------------+  |
+-------------------------------------------------------------+
```

### 4.2 核心交互亮点：
1. **接口一键连通性测试 (Live Test Connection)**：在配置面板中直接提供“测试请求”按钮，在未保存页面时也能真实发请求预览响应结构，减少盲猜。
2. **可视化 JSON Path 树级点选**：点击响应结果中的任意节点，系统自动填入 `data.records`，降低上手门槛。
3. **接口联动触发规则**：表单组件可配置“值变动时自动触发目标表格刷新”，无缝完成“搜索表单 -> 发送请求 -> 表格更新”的低代码闭环。

---

## 5. 0.1.0 里程碑实施 Roadmap

为了有序推进 0.1.0 版本的开发，划分为以下 4 个 Sprint 阶段：

```
+-----------------------------------------------------------------------------+
|                          0.1.0 里程碑开发计划                                 |
+-----------------------------------------------------------------------------+
| [阶段1] 数据源 Schema 扩展与 Pinia 全局状态解耦 (Sprint 1)                     |
|  - 扩展 PageSchema 与 ComponentNode 结构，加入 dataSources 与 apiBinding      |
|  - Pinia Store 中加入全局状态响应与统一 API 请求中心                           |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段2] 侧边抽屉 ApiBindingSetter 可视化配置组件开发 (Sprint 2)                |
|  - 开发 PropertyDrawer 中的 API 绑定配置面板                                  |
|  - 实现请求传参 Key-Value 映射器与来源选择器                                  |
|  - 开发可视化接口测试 (Live Test Api) 与 JSON 预览器                         |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段3] 核心渲染器数据映射与事件联动 (Sprint 3)                              |
|  - 开发 DynamicApiExecutor，运行时解析 DataSourceBinding 并动态请求数据       |
|  - 实现 ProTable 与 ProForm 自动接收 API 数据与搜索表单变动联动              |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段4] 全流程联调、测试与交付 (Sprint 4)                                    |
|  - 模拟真实后端 API 数据源，测试多组件联动的请求与渲染                         |
|  - 单元测试与 Playwright 自动化 UI 流程验证                                  |
+-----------------------------------------------------------------------------+
```

---

## 6. 代码质量与组件规范

1. **类型完备**：`DataSourceSchema`、`DataSourceBinding` 必须严格标注 TS 类型，禁止模糊隐式 `any`。
2. **极佳的可读性与模块解耦**：
   - 数据请求与映射逻辑独立封装在 `frontend/src/designer/executor/ApiExecutor.ts` 中。
   - 配置面板拆分为 `DataSourceSelector.vue`、`ParamMappingTable.vue`、`ResponseMapper.vue` 独立单一职责组件。

---

## 7. 低代码数据联动与接口高级创新思考 (Advanced Ideas & Innovation)

在低代码平台演进过程中，数据绑定往往会遇到极具挑战的真实业务场景。以下梳理 5 个具有高度前瞻性与实用价值的**创新功能点**，拟在 0.1.0 后续迭代中逐步引入：

### 7.1 OpenAPI / Swagger / Postman 一键导入解析 (One-Click OpenAPI Import)
- **场景痛点**：手动配置每一个 API 的 URL、Method、Params 手续繁琐且容易输错。
- **创新方案**：
  - 用户粘贴 Swagger / OpenAPI v3 JSON 或 Postman Collection 链接。
  - 平台自动解析出接口清单、字段说明、请求/响应 TS 类型，生成系统 `dataSources` 列表，用户只需点击下拉框即可选择已解析好的接口。

### 7.2 可视化无代码计算公式 (No-Code Formula / Expression Engine)
- **场景痛点**：简单的字段拼接（如 `firstName + ' ' + lastName`）、数据过滤或条件计算（如 `status === 1 ? '正常' : '禁用'`）往往需要编写 JavaScript 代码，违背“低代码”零门槛初衷。
- **创新方案**：
  - 引入类似 Excel 的可视化公式引擎（例: `CONCAT(a, b)`, `IF(status==1, '启用', '停用')`, `DATE_FORMAT(createdAt, 'YYYY-MM-DD')`）。
  - 用户只需拖拽或点选字段，系统自动完成数据预转换，无须书写任何代码。

### 7.3 智能防抖、缓存与静默请求 (Debounce, Cache & Silent Fetch)
- **场景痛点**：在输入框（如搜索框）变动时连带触发 API 刷新，若无防抖会产生大量无效 HTTP 冲突与画面闪烁。
- **创新方案**：
  - 在 `DataSourceBinding` 中增加高阶网络配置：
    - `debounce`: 防抖时间设置（默认 300ms）。
    - `cacheKey` & `cacheTTL`: 开启客户端二级缓存，对于不经常变动的字典项或下拉选项实现秒开。
    - `loadingStrategy`: 支持局部骨架屏 (Skeleton) 或静默无感更新 (Silent Refresh)。

### 7.4 模拟 Mock 数据研发模式 (Built-in Mock Server)
- **场景痛点**：后端接口未完全开发就绪时，前端可视化搭建被迫中断。
- **创新方案**：
  - 每个 API 数据源均提供【开启 Mock 模式】开关。
  - 用户可以在配置项中直接填写 Mock JSON 格式，或者基于 Mock.js 语法自动生成动态随机测试数据，让前端页面无阻碍完成布局与渲染调优。

### 7.5 错误异常处理与 Fallback UI 兜底
- **场景痛点**：后端 API 报错 500、网络超时或数据格式不符合预判时，整页崩溃或留白。
- **创新方案**：
  - 组件配置面板中提供 `errorFallback` 属性：
    - 可配置超时重试次数（Retry Limit）。
    - 接口异常时可指定显示占位 Empty 图案、友好 Toast 提示，或退回默认 Mock 兜底数据，保障生产环境健壮度。
