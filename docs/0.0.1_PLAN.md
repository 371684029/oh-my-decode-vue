# 低代码前端平台 (Low-Code Frontend Platform) - 0.0.1 规划文档

## 1. 项目概述与愿景

### 1.1 项目简介
本项目旨在打造一款高性能、高可扩展性、高度模块化的**低代码前端可视化平台**。用户可以通过可视化拖拉拽的方式快速搭建前端页面，或者将常用的 UI 与逻辑封装并导出为可复用的前端 JS/Vue 组件。

### 1.2 核心目标与首期突破点 (0.0.1 版本)
- **第一步聚焦：高端表单与高端表格拖拽生成**：
  - **高端组件库**：集成功能丰富的高端表单（多列联动、校验规则、动态增减项）与高端表格（分页、筛选、自定义列、排序、行操作列）。
  - **自动化配置生成**：用户拖拽表单/表格组件后，系统自动生成对应的标准 JSON 配置项。
- **UI 交互架构**：
  - **主内容区**：大屏可视化展示拖拽画布（基于 `vue3-grid-layout` 网格）。
  - **侧边弹框/抽屉 (Side Drawer)**：选中节点后，侧框实时呈现当前表单/表格的详细配置修改界面及 JSON 实时预览。
- **存储架构策略**：
  - **JSON 存储**：页面与组件配置项直接持久化存储为本地 `.json` 文件。
  - **SQLite 存储**：用于记录用户的操作日志 (Operation Audit Log) 及页面版本变更轨迹。
- **工程质量标准**：严谨的代码结构、强类型定义、高度组件化解耦、极致的代码可读性与可维护性。

---

## 2. 技术栈架构与选型

| 层级 | 技术选型 | 版本/组件 | 说明 |
| :--- | :--- | :--- | :--- |
| **前端框架** | Vue 3 | `3.4+` | 使用 Composition API 及 `<script setup>` 范式 |
| **UI 组件库** | Element Plus | `2.x` | 高端表单/表格物料（ElTable, ElForm, Dynamic Form 等） |
| **拖拽布局引擎** | vue3-grid-layout | `1.x` | 基于栅格网格的可伸缩拖拽布局插件 |
| **状态管理** | Pinia | `2.x` | 响应式 Store，管理设计器全局 Schema 状态 |
| **前端构建** | Vite | `5.x` | 极速冷启动与 HMR 构建工具 |
| **后端运行环境**| Node.js | `18+` / `20+` | 运行环境 |
| **后端 Web 框架**| Express | `4.x` | 轻量级 RESTful API 服务框架 |
| **数据文件存储**| File System (`.json`) | Node `fs/promises` | 存储 Schema 配置项 JSON 文件 |
| **日志数据库** | SQLite | SQLite3 / Better-SQLite3 | 存储系统操作日志、用户行为审计日志 |
| **工程化 & 规范**| TypeScript / ESLint | 最新版 | 强类型标注、严格语法检查与代码格式化 |

---

## 3. 核心设计规范与 Schema 结构

系统核心由 **JSON Schema** 驱动，实现 `Schema <-> 画布渲染 <-> 源码生成` 的双向转换。

### 3.1 页面/组件 Schema 规范 (`PageSchema`)

```typescript
export interface PageSchema {
  id: string;             // 页面或组件唯一 ID (例如: "page_001")
  title: string;          // 名称
  type: 'page' | 'component'; // 标注是完整页面还是自定义组件
  meta: {
    author: string;
    description: string;
    version: string;
  };
  // 页面全局状态与数据源定义
  state: Record<string, any>;
  // 网格布局容器及节点列表
  children: ComponentNode[];
}

export interface ComponentNode {
  id: string;             // 节点唯一 ID (如 "table_node_1")
  type: 'pro-table' | 'pro-form' | 'el-button' | 'el-input' | string; // 组件类型
  label: string;          // 物料显示名称
  // vue3-grid-layout 位置与尺寸配置
  layout: {
    x: number;            // 网格 X 坐标
    y: number;            // 网格 Y 坐标
    w: number;            // 网格宽度 (栅格列数)
    h: number;            // 网格高度 (栅格行数)
    i: string;            // 布局唯一标识 (一般同 id)
  };
  // 组件专属配置 (如表格的 columns 列配置，表单的 items 配置)
  props: Record<string, any>;
  // 高端表格/表单特有配置项定义
  config?: ProTableConfig | ProFormConfig;
  // 自定义内联样式/CSS类
  style: Record<string, any>;
  // 事件绑定 (如 onSearch, onRowClick)
  events: Record<string, EventHandler>;
  // 子节点 (支持容器嵌套)
  children?: ComponentNode[];
}

// 高端表格组件配置 Schema
export interface ProTableConfig {
  columns: {
    prop: string;
    label: string;
    width?: string;
    sortable?: boolean;
    align?: 'left' | 'center' | 'right';
    slotName?: string;
  }[];
  pagination: {
    enabled: boolean;
    pageSize: number;
  };
  actions?: {
    label: string;
    type: string;
    eventKey: string;
  }[];
}

// 高端表单组件配置 Schema
export interface ProFormConfig {
  layout: 'inline' | 'horizontal' | 'vertical';
  labelWidth: string;
  items: {
    field: string;
    label: string;
    component: 'input' | 'select' | 'date' | 'switch' | 'radio';
    placeholder?: string;
    rules?: any[];
  }[];
}

export interface EventHandler {
  type: 'custom_js' | 'page_jump' | 'open_modal' | 'refresh_table';
  code?: string;          // 自定义 JS 代码段
  target?: string;
}
```

---

## 4. 前后端工程目录结构与交互架构

### 4.1 目录结构

```text
low-code-platform/
├── docs/                     # 项目规划与架构设计文档
│   └── 0.0.1_PLAN.md
├── frontend/                 # Vue 3 前端工程
│   ├── src/
│   │   ├── assets/           # 静态资源
│   │   ├── components/       # 高端通用 UI 组件 (ProTable, ProForm)
│   │   ├── designer/         # 低代码设计器核心模块
│   │   │   ├── canvas/       # 主内容区画布引擎 (集成 vue3-grid-layout)
│   │   │   ├── materials/    # 左侧物料库 (表单、表格等组件)
│   │   │   ├── drawers/      # 侧边配置弹框/抽屉 (表格/表单配置面板与 JSON 预览)
│   │   │   └── generator/    # 源码生成器引擎 (Schema -> Vue 3 Code)
│   │   ├── registry/         # 物料元数据注册中心
│   │   ├── stores/           # Pinia 状态管理 (designer store)
│   │   ├── views/            # 页面路由视图
│   │   ├── types/            # TypeScript 类型定义
│   │   └── utils/            # 工具函数
│   ├── package.json
│   └── vite.config.ts
├── backend/                  # Node.js + Express 后端服务
│   ├── src/
│   │   ├── config/           # 配置文件
│   │   ├── db/               # SQLite 操作日志表初始化脚本
│   │   ├── controllers/      # 业务控制器 (JSON 文件 CRUD + 日志记录)
│   │   ├── routes/           # RESTful API 路由
│   │   ├── services/         # 文件读写服务 (fs) 与 SQLite 日志服务
│   │   └── types/            # 类型定义
│   ├── storage/              # JSON 存储目录
│   │   ├── pages/            # 页面 JSON 配置集 (*.json)
│   │   └── components/       # 组件 JSON 配置集 (*.json)
│   ├── data/                 # SQLite 数据库目录
│   │   └── logs.db           # 操作日志与审计数据库
│   ├── package.json
│   └── server.js
├── package.json              # Monorepo / Root Scripts
└── README.md
```

---

## 5. 存储设计：JSON 文件存储 + SQLite 操作日志表

### 5.1 JSON 文件存储路径
- 页面 Schema 存储位置：`backend/storage/pages/{page_id}.json`
- 导出的组件 Schema 存储位置：`backend/storage/components/{component_id}.json`

### 5.2 SQLite 数据库数据模型：`operation_logs` (操作日志表)
| 字段名 | 类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 日志主键 |
| `page_id` | VARCHAR(36) | NOT NULL | 关联的页面/组件 ID |
| `action` | VARCHAR(50) | NOT NULL | 操作类型 (`CREATE_PAGE`, `UPDATE_SCHEMA`, `DELETE_PAGE`, `EXPORT_CODE`) |
| `operator` | VARCHAR(50) | NOT NULL DEFAULT 'system' | 操作者身份 |
| `details` | TEXT | NULL | 操作变动细节描述（JSON 格式） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 日志记录时间 |

---

## 6. 0.0.1 里程碑实施 Roadmap

```
+-----------------------------------------------------------------------------+
|                          0.0.1 里程碑开发计划                                 |
+-----------------------------------------------------------------------------+
| [阶段1] 脚手架搭建与 JSON/SQLite 双存储设计 (Sprint 1)                          |
|  - 前后端基础工程搭建 (Vite+Vue3+TS, Node+Express)                         |
|  - 后端 JSON 文件读写 API 实现 (`/api/schemas`)                              |
|  - 后端 SQLite 操作日志表初始化 (`operation_logs`)                           |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段2] 主画布拖拽与高端表单/表格物料库 (Sprint 2)                             |
|  - 基于 vue3-grid-layout 搭建主内容展示区                                     |
|  - 拖拽实现：侧边物料拖拽投射至主画布                                         |
|  - 高端物料开发：ProTable (带列定义、排序、分页) 与 ProForm (多列、规则定义)    |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段3] 侧边抽屉配置面板与 JSON 联动 (Sprint 3)                                |
|  - 选中画布中的组件后，自动拉起侧边配置抽屉/弹框                              |
|  - 提供表单列/表格列的可视化新增、修改与排序                                   |
|  - 实时更新渲染主画布对象，并同步更新侧边 JSON 源码预览                         |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段4] 配置项直接写入 JSON 文件与操作日志留痕 (Sprint 4)                       |
|  - 点击保存将最新配置写入后端 `.json` 文件                                     |
|  - 异步向 SQLite `operation_logs` 插入操作记录                                |
+-----------------------------------------------------------------------------+
                                     │
                                     ▼
+-----------------------------------------------------------------------------+
| [阶段5] Vue 3 组件出码引擎与验收交付 (Sprint 5)                               |
|  - Schema -> 标准 Vue 3 代码生成器 (集成 ProTable / ProForm 逻辑)            |
|  - 支持导出独立 `.vue` 源码文件及纯 JS 组件模块                                 |
|  - 代码重构、样式精细化与质量自检                                             |
+-----------------------------------------------------------------------------+
```

---

## 7. 代码与质量规范 (Quality & Style Guide)

1. **强类型与严谨声明**：
   - 所有 Schema 数据结构与配置项必须具有完备的 TypeScript interface / type 定义。
   - 杜绝隐式 `any`，确保配置变更时有完备的编译时类型检查与代码提示。

2. **严格组件化与单职责原则 (SRP)**：
   - 界面严格解耦：
     - `CanvasContainer`: 主内容渲染画布
     - `ProTableWidget` / `ProFormWidget`: 运行时高端组件包装器
     - `PropertyDrawer`: 侧边配置弹框/抽屉
     - `SchemaJsonViewer`: 配置项 JSON 源码展示器
   - 物料属性配置项采用工厂机制，保持高扩展性。

3. **可维护性与扩展性**：
   - 新的高端表单/表格控件接入只需在 `registry` 中增加配置 Schema 与渲染组件，无须改动主引擎代码。
   - 前后端代码排版规范统一（ESLint + Prettier），代码工整，多写注释。
